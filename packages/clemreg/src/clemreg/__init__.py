"""clemreg — core registration library for CLEM-Reg.

This is a *placeholder* release (0.0.0) that reserves the ``clemreg`` name on
PyPI. The functionality currently lives in the ``napari-clemreg`` package under
``napari_clemreg.clemreg`` and will be extracted into this standalone,
napari-free core package (segmentation, point-cloud sampling, registration,
image warping and MoBIE export).

For working software today, install the napari plugin::

    pip install napari-clemreg

See the project repository for the modernisation and package-split plan.
"""

__version__ = "0.0.0"
